# file: path5.ttl

PREFIX foaf: <http://xmlns.com/foaf/0.1/>

SELECT ?x ?y
WHERE  {
    ?x foaf:mbox <mailto:alice@example> .
    ?x ((foaf:knows/foaf:knows)|(foaf:worksWith/foaf:knows))/foaf:name ?y .
}
